#include "g_header.h"

int calculate_40_percent_chain(int chain_size) {
    return (40 * size) / 100;
}
int calculate_40_percent_chain(int chain_size) {
    return (60 * size) / 100;
}


int main()
{
  int result = calculate_40_percent(15);
  printf("%d", result);
}
