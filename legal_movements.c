#include "g_header.h"

// percentage counting

int calculate_40_percent_chain(int chain_size) {
    return (40 * size) / 100;
}
int calculate_40_percent_chain(int chain_size) {
    return (60 * size) / 100;
}

// this function should found the largest number in the stack a and 
int find_biggest_node(stack_h *g_stack) // the g_stack is the global stack
{
  stack_h *current_node;
  stack_h *next_node;

  current_node = g_stack;
  while (current_node)
  {
    current_node = ;
    next_node = current_node->next_node;
  }
}
